<p id="notice"><%= notice %></p>

<h1>AirRescue</h1>
   <br>
   <br>

    <% @nodes.each do |node| %>
        <div class="row">
          <div class="col-md-2"><%= node.name %> <br> <%= node.data %></div>
          <% if user_signed_in? %>
              <div class="col-md-2"><%= link_to 'Show', node %> <br> <%= link_to 'Edit', edit_node_path(node) %> <br> <%= link_to 'Destroy', node, method: :delete, data: { confirm: 'Are you sure?' } %></div>
          <%else%>      
              <div class="col-md-2"><%= link_to 'Show', node %> </div>
          <%end%>
          
        </div>
        <br>
        <br>
    <% end %>
    
      <% if user_signed_in? %>
        <%= link_to "cerrar sesion", destroy_user_session_path, method: :delete %>
        <%= link_to 'New Node', new_node_path %>
         <br>
      <%else%>
        <%= link_to "Iniciar sesion", new_user_session_path %>
      <%end%>

<br>

